{
  "name": "lifeos",
  "version": "0.0.0",
  "private": true,
  "description": "LifeOS — Gêmeo Digital da Vida. Um jogo de navegador em pixel art que espelha a vida real do usuário.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "dexie": "^4.0.11",
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "fake-indexeddb": "^6.0.1",
    "typescript": "^5.7.3",
    "vite": "^6.3.6",
    "vitest": "^3.2.4"
  }
}
